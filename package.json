{
  "name": "root",
  "version": "1.50.0",
  "devDependencies": {
    "lerna": "^3.22.1",
    "semantic-release": "^17.1.1"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build:core": "cd ./packages/ui-library && npm run build",
    "build:vue": "cd ./packages/ui-library-vue && npm run build",
    "build": "npm run build:core && npm run build:vue",
    "semantic-release": "semantic-release"
  }
}
